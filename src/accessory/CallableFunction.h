////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
// 回调函数集，用于需要谓词的函数中。
//
// 为了隐藏实现细节，具体的定义在对应的cpp文件中。
//
// 下列函数主要用于数列的排序。本系统使用的数列中，一般存储的是各数据的智能指针，无法使用元素本身的<操作符来排序，
// 只能使用外定义的谓词形式。故而需要定义下列函数。
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#pragma once

#include"ClassDeclaration.h"

[[nodiscard]] bool CompareDayLineDate(const CDayLinePtr& p1, const CDayLinePtr& p2);
